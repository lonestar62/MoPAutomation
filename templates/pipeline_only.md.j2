---
title: {{ title }}
id: {{ id }}
category: {{ category }}
region: {{ region }}
playbooks:
{% for pb in playbooks %}  - {{ pb }}
{% endfor %}
parameters:
  pipeline_name: {{ pipeline_name | default('manual-deployment') }}
  pipeline_parameters: {{ pipeline_parameters | default({}) }}
  wait_for_completion: {{ wait_for_completion | default(true) }}
---

# {{ title }}

## Overview
This MOP triggers Azure DevOps pipeline execution for {{ region }} region operations.

## Pipeline Configuration
- **Pipeline Name**: {{ pipeline_name | default('manual-deployment') }}
- **Region**: {{ region }}
- **Wait for Completion**: {{ wait_for_completion | default(true) }}

## Pipeline Parameters
{% if pipeline_parameters and pipeline_parameters.items() %}
{% for key, value in pipeline_parameters.items() %}
- **{{ key }}**: {{ value }}
{% endfor %}
{% else %}
- No additional parameters specified
{% endif %}

## Pre-execution Checklist
- [ ] Verify pipeline exists and is enabled
- [ ] Confirm Azure DevOps permissions
- [ ] Check required pipeline parameters
- [ ] Validate target environment readiness
- [ ] Review change approval requirements

## Execution Process

### 1. Pipeline Trigger
The system will:
- Connect to Azure DevOps organization
- Locate pipeline: `{{ pipeline_name | default('manual-deployment') }}`
- Submit execution request with parameters
- Monitor pipeline queue status

### 2. Execution Monitoring
{% if wait_for_completion | default(true) %}
- Real-time pipeline status monitoring
- Stage-by-stage progress tracking
- Log collection and analysis
- Error detection and reporting
{% else %}
- Pipeline triggered asynchronously
- Manual monitoring required
- Check Azure DevOps portal for status
{% endif %}

### 3. Result Validation
- Pipeline completion status verification
- Output artifact validation
- Deployment health checks
- Success criteria evaluation

## Associated Playbooks
{% for playbook in playbooks %}
- `{{ playbook }}`
{% endfor %}

## Expected Timeline
- **Pipeline Queue Time**: 1-5 minutes
- **Execution Duration**: {{ estimated_duration | default('10-30') }} minutes
- **Validation**: 2-5 minutes

## Success Criteria
- Pipeline successfully triggered
{% if wait_for_completion | default(true) %}
- Pipeline execution completes successfully
- All stages pass validation
{% endif %}
- No critical errors reported
- Target environment remains stable

## Monitoring Resources
- **Azure DevOps Portal**: {{ devops_url | default('[Organization URL]') }}
- **Pipeline Dashboard**: Direct link to execution
- **Logs Location**: Pipeline execution logs
- **Metrics**: Performance and success metrics

## Troubleshooting
Common issues and resolutions:
- **Queue Timeout**: Check pipeline capacity and agent availability
- **Permission Errors**: Verify service principal permissions
- **Parameter Validation**: Review required vs provided parameters
- **Execution Failures**: Check pipeline logs and environment status

## Post-execution Actions
- Review pipeline execution logs
- Verify deployment success
- Update change management records
- Document any issues or observations

## Additional Information
- **Change Reference**: {{ change_reference | default('TBD') }}
- **Business Justification**: {{ business_justification | default('Operational requirement') }}
- **Risk Level**: {{ risk_level | default('Medium') }}

---
*Generated by MOP Ansible Renderer for Azure DevOps integration*
